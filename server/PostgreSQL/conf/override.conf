# Enforce synchronous replication to avoid data loss
synchronous_commit = on
# Wait for at least one of these standbys (by application_name) to confirm each commit
synchronous_standby_names = 'FIRST 1 (postgresql-replica-2, postgresql-replica-3)'
